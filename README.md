# CloneTray

A macOS menu bar app that clones a Git repository and opens it in your editor. It
pre-fills the URL from the tab you were just looking at, so cloning a repo that's on
screen is two clicks.
